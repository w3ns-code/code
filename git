//First of all
git init
git remote add origin https://github.com/w3ns-code/code.git
git branch -M main

// QUAND TU COMMENCES tu veux être sur ta BRANCHE : 
git branch pour vérifier avant

// Puis par la suite, pour changer la branche sur laquelle tu veux travailler
git checkout c'est  

//Avant de commencer à coder faire cette commande pour récupérer le travail dépuis la branche test.
git pull origin/(test)

(Vous pourrez travailler)

//Faire ça après d'avoir fini de coder
git add .
//Commit c'est pour confirmer et enregistrer ces changements localement dans Git laisser un message qui décrirera concrètement ce que vous avez faites
git commit -m "Ajout du formulaire d'ajout"
git push origin branche-nom1

git checkout test
git pull origin (test)
git merge origin (ta branche-nom1)

git push origin test


problem we might encounter :
git push origin main
To https://github.com/w3ns-code/code.git
 ! [rejected]        main -> main (non-fast-forward)
error: failed to push some refs to 'https://github.com/w3ns-code/code.git'
hint: Updates were rejected because the tip of your current branch is behind
hint: its remote counterpart. If you want to integrate the remote changes,
hint: use 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.

solution :
git pull origin main
git merge main
git push origin main

OR 

git push origin main --force 

